import React from 'react';
import { Link } from 'react-router-dom';

const Home = (props) => {

    return (
        <div>
            Traveller Home
            {/* <Link to={`/trips/${trip.id}`}>{trip.name}</Link> */}
        </div>
    )
}

export default Home;